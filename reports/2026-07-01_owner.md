# AYCB v2 — Session Report 2026-07-01

## What We Did
Added Google's two newest models and refreshed pricing. New **Nano Banana 2 Lite**
image model (fastest/cheapest Gemini image) and **Gemini Omni Flash** video model
(720p, 3–10s, animate photos into video). Renamed the existing Nano Banana 2 /
Pro image models to Google's new official ids while keeping old saved projects
working. Fixed a wrong async batch price.

## What Changed
- **Nano Banana 2 Lite** — pick it from the image node or type `nb2lite`. 1K only
  (Google doesn't support higher for this model — verified live), ~$0.034/image,
  half that in async batch.
- **Gemini Omni Flash** — new video model, pick it or type `omni`. Text→video and
  photo→video, up to 5 reference photos, $0.10/second.
- **Pricing** — all Google image/video prices matched to Google's official page.
  Fixed the Lite async price that was showing full price instead of the −50%.
- Old projects using the previous model ids keep working automatically.

## Current State
Everything builds and all automated tests pass. Committed to the `dev-full`
branch (checkpoint `e8fd44d`). One thing is **not yet confirmed on the live API**:
the Omni video model — the code follows Google's docs but hasn't been run against
the real service yet.

## Next Session
1. Run the Omni video smoke test with your key to confirm it actually generates
   (I wrote the script). Fix field names if Google's real response differs.
2. Then it's ready to use for real video jobs.
