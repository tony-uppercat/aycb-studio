import type { NodeManifest } from '../_shared/types'

const manifest: NodeManifest = {
  type: 'console',
  label: 'Console',
  icon: '📋',
  category: 'utility',
  description: 'Display backend logs in real time',
  defaultData: {},
  inputs: [],
  outputs: [],
}

export default manifest
