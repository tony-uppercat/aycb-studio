# AYCB v2 — Session Report 2026-04-13

## What We Did
Fixed the Bracket Parser node — edit/override values weren't reaching the output, neither from connected pins nor from inline UI edits.

## What Changed
- Bracket Parser now correctly propagates pin-connected values and manual overrides to both the template output text and per-pin outputs downstream.

## Current State
Bracket Parser override/pin flow works. 32 bracket parser tests pass. All prior features intact.

## Next Session
1. Ctrl+Alt drag copy: original stays, copy gets dragged
2. 2K default resolution in Generate Image
3. Pricing display in model dropdown
4. Selection issue investigation
